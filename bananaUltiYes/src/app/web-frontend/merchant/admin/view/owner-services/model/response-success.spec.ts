import { ResponseSuccess_cl } from './response-success';

describe('ResponseSuccess_cl', () => {
  it('should create an instance', () => {
    expect(new ResponseSuccess_cl()).toBeTruthy();
  });
});
